# Backend

The backend contains the FastAPI application, replaceable support-agent services, preprocessing scripts, vector index, and evaluation contracts. Run commands from the repository root unless noted otherwise.

See the root `README.md` for setup and the end-to-end workflow.
